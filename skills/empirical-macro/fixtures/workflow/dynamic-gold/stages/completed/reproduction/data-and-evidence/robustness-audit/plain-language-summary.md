# 已声明稳健性检查

在已声明且实际完成的检查范围内，没有观察到超过冻结判据的敏感性。这不证明识别假设成立，也不覆盖未执行的规格。

## 检查状态

covariance_sensitivity=passed；exact_rerun=passed；lag_sensitivity=passed；sample_policy_sensitivity=passed；sample_window_sensitivity=passed。

该审计只描述计划中列明的检查，不代表所有可能规格。
