interface:
  display_name: "宏观经济实证动态研究"
  short_description: "路由并执行当前支持的宏观动态研究工作流"
  default_prompt: "Use $empirical-macro for supported end-to-end empirical macro research. Call empirical-macro kernel.run() as the total controller; do not manually compose route, research-design, and decide_after_stage calls. A stopped result ends that workflow run and cannot be presented as validated research output; the host decides how to handle later user input. Never mutate WorkflowState directly. Only dynamic_shock_response and conditional_dynamic_association are executable. For every unsupported method, return exactly 当前版本不能执行该方法 and no additional user-facing content."
